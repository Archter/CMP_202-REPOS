package src.User;

public class User {
    String name;
    String password;
    String login() {
        return "logged In";
    }

    void Regin() {
        if (name.isEmpty() || password.isEmpty()) {
        } else {
        }
    }
}
